__pycache__/
*.pyc
.DS_Store
.venv/
.claude/

# docs/ and data/ are deliberately NOT ignored: brief.yml commits them, and
# the repo is the archive. Only local fixture art is excluded.
docs/_fixture/
